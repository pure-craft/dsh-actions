/** Vitest alias stub for @iconify/react (see primitives.ts stub rationale). */
import * as React from 'react';
export const Icon = (): React.ReactElement => React.createElement('span');
export default Icon;
